# Runtime secrets & state — generated/held locally, NEVER commit these.
vault/tls/
.vault-init.json
.kms-token
.kp-root-pw
# adminKP config + provisioning artefacts
adminkp.env
known_hosts
# encrypted backups (T7: kms-data-*, kms-unseal-*)
*.tar.gz.gpg
